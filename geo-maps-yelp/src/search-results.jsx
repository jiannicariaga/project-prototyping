import React from 'react';

export default class SearchResults extends React.Component {
  render() {
    // Render search results passed in from Search

    return (
      <section>

      </section>
    );
  }
}
